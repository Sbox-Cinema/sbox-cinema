@using Sandbox;
@using Sandbox.UI;

@namespace Cinema.UI
@attribute [StyleSheet( "/ui/hud/interact/InteractPrompt.scss" )]
@inherits Panel

<root>
    
    <div class="prompt @(Hidden ? "hidden" : "")">
        <div @ref=UseBar class="long-use-bar" />
        <div class="label">@PromptText</div>
    </div>
</root>
@using Sandbox.UI;

@namespace Cinema.UI
@inherits Panel;
@attribute [StyleSheet]

<root>
    <div class="left">
        <div class="priority">## @Priority</div>
        <div class="thumbnail" style="background-image: url(@MediaInfo.Thumbnail );" />
    </div>
    <div class="details">
        <div class="title">@MediaInfo.Title</div>
        <div>
            <div class="duration">@MediaInfo.DurationFormatted</div>
            <button class="icon upvote" @onclick=@(() => OnVote(true))>arrow_upward</button>
            <button class="icon downvote" @onclick=@(() => OnVote(false))>arrow_downward</button>
            <button class="icon remove" @onclick=@OnRemove>delete</button>
        </div>
    </div>
</root>
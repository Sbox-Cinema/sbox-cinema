@inherits Panel
@using Sandbox;
@using Sandbox.UI;
@using Cinema;

@attribute [StyleSheet]
<root>

</root>

@code {
    public void RefreshVideoProviders()
    {
        var providers = VideoProviderManager.Instance.GetAll();
    }
}